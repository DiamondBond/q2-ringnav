Wheel navigation in the search result dialogs (to be implemented)
-------------------------------------------------------------------
The only remaining reachable list surfaces where the wheel falls back to
volume are the local/online search dialog and the Tidal search dialog.

- Add to patch/contexts.inc as dynamic contexts (a new query is new content,
  so no position restore):
    { "search_dialog", CTX_DYNAMIC },
    { "tidal_search_dialog", CTX_DYNAMIC },
- Do NOT add "searchbox_dialog": it has no navigable pane, and allowlisting a
  pane-less window makes the wheel get consumed instead of changing volume.
- Tests: a wheel/centre scenario on a page named "search_dialog", plus both
  names in the dynamic (fresh-start) scenarios in tools/test_patch.py.
- Device check: each dialog is one outer scroll view that also holds the tab
  buttons, so the first detent may land on the Track/Artist/Album/Genre row
  before the results. If that feels wrong on hardware, refine surface_under to
  prefer a pane that has entries when neither pane owns the selection.
- On release: changelog entry for the new version.

Artist → Albums default
-----------------------
- Open Artist detail on Albums by default; retain the All Songs tab and its
  selection. Audit g_artist_type/tab initialization and saved preference behavior.
- Verify empty artists, one/many albums, tab switching and Back on device.

Gentler touch inertia
---------------------
- Measure stock swipe coast on short and long local lists, then tune touch
  inertia without changing wheel acceleration or interrupt-on-wheel behavior.
- Check overshoot, end stops and touch-to-wheel handoff on device.

Article-aware artist sorting
----------------------------
- Add artist sort keys that ignore leading articles without changing display
  names. Define supported languages/articles and tie-breaking first.
- Test article-only names, punctuation, diacritics and selection restoration.

Localization verification
-------------------------
- Check supported languages on normal and compact, including long labels,
  two-line metadata, retained tabs/actions and missing translation fallbacks.
- Record device screenshots and fix clipping without shrinking stock fonts.

Quick-settings volume access
----------------------------
- Audit the stock quick-settings surface for a direct volume entry that remains
  reachable during wheel navigation. Reuse the stock volume dialog.
- Verify touch/physical access, locked screen behavior and return to browsing.

Deferred Home short Return shortcut
----------------------------------
- Keep short Return stock on Home. Revisit only as a separate control change;
  compact V3.1 uses long Return to Now Playing.
